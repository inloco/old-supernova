machine:
  node:
    version: 6.2.1

dependencies:
  cache_directories:
    - node_modules
  override:
    - npm install

test:
  override:
    - npm test -- --coverage
  post:
    - mkdir -p $CIRCLE_ARTIFACTS/coverage-test
    - mv coverage/lcov-report $CIRCLE_ARTIFACTS/coverage-test
